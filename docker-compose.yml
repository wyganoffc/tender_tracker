version: '3.8'

services:
  db:
    image: postgres:15
    container_name: tender_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tenders
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: tender_app
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/tenders
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    command: uvicorn app.routers:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data: